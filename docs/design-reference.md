# Design Reference Pointer

Populate this file with:
- The GitHub URL of `GmailAnalysis-Design`
- The exact commit SHA that is the current design baseline
- The authoritative manifest path:
  - `docs/turnover-manifest.md`

This keeps the implementation repo synchronized with design decisions.
